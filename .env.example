# Sao chép file này thành .env rồi điền giá trị thật
# cp .env.example .env

# Port server lắng nghe (ngrok/Railway tự set, không cần đổi)
PORT=3000

# Chuỗi bí mật để ký JWT — đổi thành chuỗi ngẫu nhiên dài khi deploy thật
JWT_SECRET=minesweeper_secret_key_2024
